# Temu 摩托配件评论抓取：运营使用说明

## 推荐入口：Temu选品运营台

1. 双击 `启动Temu运营台.vbs`，系统会在浏览器中打开本地运营台，不显示黑色命令窗口。
2. 点击“打开采集 Chrome”。这是独立、长期保留登录状态的普通 Google Chrome，不带 `--no-sandbox`。
3. 在该 Chrome 中人工登录 Temu，确认德国站 / 英语 / EUR，进入 `Automotive → Motorcycles & Powersports Accessories`，并手动选择 `Sort by: Top sales`。
4. 看到真实商品列表后，回到运营台点击“采集当前页面”。程序只读取当前页面，不会自己跳转旧链接、搜索或反复刷新。
   - 页面只有首批约 40 个商品时，程序会在数量停止增长后受控点击 `See more`，低频加载到目标数量；最多点击 4 次，遇到验证或网络异常立即暂停。
5. 商品池采集成功后，在采集 Chrome 的 Top Sales 列表中人工点开第1个商品；确认商品详情和评论区域正常显示。
6. 回到运营台点击“采集当前商品”。脚本只读取运营已经打开的当前页，不自动跳转、搜索或刷新商品链接。
7. 完成后返回 Top Sales 列表，按页面名次打开下一个商品，再点击“采集下一个当前页”；重复运行自动去重。
8. 任务完成后可直接点击“打开运营 Excel”或“打开结果文件夹”。测试前如需清空报表，点击“清除 Excel 内容”；数据库不会删除。

运营台地址固定为 `http://127.0.0.1:37821`，只在本机访问。CMD 文件仅保留作开发维护和紧急兜底，运营日常不需要使用。

## 当前页采集的安全规则

- 当前页面不是 Temu、不是摩托配件或没有商品时立即停止。
- 页面没有明确显示 `Sort by: Top sales` 时立即停止，不会替运营人员自动修改排序。
- 一次少于 20 个商品时不更新商品池，避免把网络异常或未加载完整的页面当成真实结果。
- 只有全部检查通过后才更新 SQLite；任一检查失败，原商品池和历史评论都保持不变。
- 滚动采用低频随机间隔，每批目标 50–100 个，不绕过验证码或安全验证。
- 没有成功采集当前商品池时，运营台会禁止继续抓评论，避免处理大批已经失效的旧链接。
- 评论阶段同样采用当前页模式：运营先打开真实商品，脚本再收集当前页；程序不会根据数据库旧链接自动导航商品详情。
- 当前商品不属于已采集的 Top Sales 商品池时立即停止，避免把其他类目商品混入。
- 当前页异常、无评论、验证或抓取失败时只更新该商品状态，不影响已经完成的商品。

## CMD兜底操作

1. 首次使用或需要更新商品时，优先使用运营台。仅在运营台无法启动时双击 `0-刷新TopSales商品池并导表.cmd`。
2. Chrome 打开后，确认站点为德国、语言为英语、币种为 EUR，并停留在摩托配件商品列表；然后回到黑色窗口按 Enter。程序会自动确认 `Top Sales` 排序并收集当前商品。
   - 如果程序提示网络或 VPN 错误，可在这个 Chrome 中处理网络，也可以人工进入 `Automotive → Motorcycles & Powersports Accessories` 商品列表；确认商品正常显示后再按 Enter。
3. 商品池刷新完成后，双击 `1-抓取下一批评论并导表.cmd`。
4. 程序会停在第一个有效商品页，并在黑色窗口明确提示“评论抓取已暂停”。
5. 在该 Chrome 中完成登录、验证码或安全验证，确认商品详情和评价区域正常显示；然后回到黑色窗口按 Enter。
6. 如果页面提示网络或VPN异常，程序会再次暂停；处理好后按 Enter，程序会重新加载当前商品。
7. 抓取运行期间不要手动关闭整个 Chrome 窗口；程序只复用一个商品标签页，避免窗口反复闪烁。
8. CMD批量方式仅保留作开发兜底；运营日常应采用“人工打开商品 → 采集当前商品”的半自动流程。
9. 每次当前页采集完成后脚本会自动重新生成 Excel；打开报表查看“评论明细”“差评主题分析”和“评论抓取进度”。

## 异常处理

- 个别商品失败不会让整批停止；双击 `2-重试失败评论并导表.cmd` 重试。
- 只想刷新 Excel 时，双击 `3-仅重新导出Excel.cmd`。
- 运营台“清除 Excel 内容”必须二次确认；如果 Excel/WPS 正在打开，会提示先关闭文件，不会改写数据库。
- 如果固定名称的运营 Excel 正在被 Excel/WPS 占用，系统会自动另存一个带时间戳的新文件；运营台“打开运营 Excel”会打开最新版本。
- 不要删除 `data\temu_week1.db`，它是商品、评论和抓取进度的数据底座。
- 采集商品池成功后，旧链接只退出当前运营队列，历史数据不会被删除；如果采集失败，原商品池保持不变。
- 不要把 `browser-profile-fresh` 目录发给其他人，其中可能包含登录会话。

## 数据口径

- 当前报表只包含真实 Temu 商品，演示数据已排除。
- 类目商品统一使用 `Top Sales` 口径；前10验收按页面名次执行，不按销量重新排序。
- 商品主图以图片方式嵌入 Excel，不依赖单元格中的图片 URL。
- 已售罄商品会标记并跳过；临时网络错误和选择器错误会保留为失败项供重试。
- “最早可见评论日期”和“估算上架区间”都来自当前可见评论，不是平台官方上架日期。
- 程序不会绕过验证码，也不会自动提交或上架任何商品。

## 阶段门

- 前10个：至少8个商品评论采集成功；重复运行不产生重复评论；异常按售罄、无评论、验证码、网络、受限和链接失效分别记录。
- 100个：前10通过后再做近30天+部分历史评论的稳定性验证。
- 1000个：只采集商品基础指标，不深抓评论。
- 市场分析后筛2–5个子类、每类10–30个重点商品深抓；人工确认3–10个候选品后再做1688寻源。
- 运营Excel重新导出时，按Temu商品链接保护人工备注、竞品、1688和供应商字段。
